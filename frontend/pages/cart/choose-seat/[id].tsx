import Layout from '../../../components/cart/layout';

export const ChooseSeat = () => {
    return (
        <Layout>
            <div>Job is done</div>
        </Layout>
    );
};

export default ChooseSeat;
